<nav class="navbar navbar-expand-lg fixed-top navbar-dark navbar-left bg-dark">
    <!--- Logo --->
    <a class="navbar-brand" href="dashboard">
        <img src="../images/dragon.svg" width="25vh" height="25vh" />
        <b>SMAUG</b>budget
    </a>

    <!--- Logo --->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!--- Navbar Links --->
    <div class="button collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link {{#if dashboard}}active{{/if}}" href="dashboard">DASHBOARD{{#if dashboard}}<span class="sr-only">(current)</span>{{/if}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if envelopes}}active{{/if}}" href="envelopes">ENVELOPES{{#if envelopes}}<span class="sr-only">(current)</span>{{/if}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if goals}}active{{/if}}" href="goals">GOALS{{#if goals}}<span class="sr-only">(current)</span>{{/if}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if bills}}active{{/if}}" href="bills">BILLS{{#if bills}}<span class="sr-only">(current)</span>{{/if}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if history}}active{{/if}}" href="#">HISTORY{{#if history}}<span class="sr-only">(current)</span>{{/if}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{#if utility}}active{{/if}}" href="utility">UTILITIES{{#if utility}}<span class="sr-only">(current)</span>{{/if}}</a>
            </li>
        </ul>

        <!--- Navbar Profile Dropdown Menu  --->
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink-4" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <b class="fas fa-user"></b> User </a>
            <div class="dropdown-menu dropdown-menu-right dropdown-info" id="dropdown-user" aria-labelledby="navbarDropdownMenuLink-4">
                <a class="dropdown-item" href="account"><i class="fas fa-cog"></i>Settings</a>
                <a class="dropdown-item" href="#" onclick="toggleDarkMode();loadGraphs();" id="appearance"><i class="fas fa-adjust"></i>Appearance: Light</a>
                <hr>
                <a class="dropdown-item" href="/"><i class="fas fa-sign-out-alt"></i>Log out</a>

            </div>
            </li>
        </ul>
    </div>
</nav>
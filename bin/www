/*
 * Entery point for SmaugBudget server 
 */

const config = require('../app_server/config/server.json');

var server = require('../app_server/routes/controller.js');
server.startServer(config.socket.port);
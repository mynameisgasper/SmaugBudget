/*
 * Entry point for SmaugBudget server 
 */

//Dependencies
const config = require('../app_server/config/server.json');
var server = require('../app');

//Express server
server.startServer(config.server.port);